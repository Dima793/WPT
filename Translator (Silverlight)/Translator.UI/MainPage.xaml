<Page
    x:Class="Translator.UI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Translator.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Translator.UI"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=Microsoft.Phone"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:ClassModifier="internal">

    <navigation:Page.DataContext>
        <ui:MainPageViewModel/>
    </navigation:Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Command="{Binding StartListenUserSpeech}" 
        Content="{Binding StartButtonContent}"
            Width="130"
            Height="100" Margin="140,110,0,110"    />

        <Button Grid.Row="1" Command="{Binding StopListenUserSpeech}" 
        Content="{Binding StopButtonContent}"
            Width="130"
            Height="100" Margin="140,110,0,110"    />
    </Grid>
</Page>
